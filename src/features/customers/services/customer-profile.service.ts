import { supabase } from "@/shared/lib/supabase"; export async function updateCustomerProfile( id: string, data: { first_name: string; last_name?: string; avatar?: string | null; date_of_birth?: string | null; } ) { const { data: updatedCustomer, error } = await supabase.rpc( "update_customer_profile", { p_customer_id: id, p_first_name: data.first_name, p_last_name: data.last_name ?? null, p_avatar: data.avatar ?? null, p_date_of_birth: data.date_of_birth ?? null, } ); if (error) { throw error; } return updatedCustomer; }